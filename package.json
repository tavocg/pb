{
  "name": "app",
  "version": "0.1.0",
  "description": "App description",
  "license": "GPL-3.0-or-later",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "redocly": "mkdir -p handlers/static/api && redocly build-docs docs/api.yaml --output handlers/static/api/index.html >/dev/null 2>&1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@redocly/cli": "^2.30.3"
  }
}
