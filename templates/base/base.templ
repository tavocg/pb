package base

type PageParams struct {
	Head HeadParams
	Body BodyParams
}

templ Page(tr func(string, ...any) string, page PageParams) {
	<!DOCTYPE html>
	<html>
		@head(page.Head)
		@body(tr, page.Body)
	</html>
}
